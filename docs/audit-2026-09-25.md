# Independent Repository Audit — 2026-09-25

## Baseline and boundaries

- Source: default branch `main`, commit `48089e06d896caad35899e8e5792810090ea24a8`.
- Work takes place in a fresh clone on `audit/sc300-evidence-2026-09-25`; existing user checkouts are untouched.
- No repository or applicable ancestor `AGENTS.md` was found.
- Baseline inventory: 333 tracked files — 61 Markdown, six PowerShell, six KQL, one SVG, `.gitignore`, and 258 original screenshots (255 PNG, three JPG).
- Review the current files and original screenshots independently. Earlier review conclusions are inputs to check, not evidence.
- No Entra/Azure connection, tenant write, or live query is part of this audit. Local checks do not create a new lab Pass result.
- Preserve every screenshot byte and its masking; a baseline SHA-256 inventory is retained in the local audit workspace for final comparison.

## Checkpoints

| Stage | State | Record |
| --- | --- | --- |
| Baseline and inventory | Complete | Default branch, source commit, clean clone and file inventory recorded above |
| PowerShell and KQL | Complete | Six scripts parsed; 54 isolated assertions passed; all six KQL queries passed local syntax/semantic analysis |
| Days 01–06 | Complete | [60/60 images and 18 day text files](audit-evidence-days-01-06.md) |
| Days 07–12 | Complete | [86/86 images and 18 day text files](audit-evidence-days-07-12.md) |
| Days 13–18 | Complete | [112/112 images and 18 day text files](audit-evidence-days-13-18.md) |
| Cross-file consistency and Microsoft Learn | Complete | README, access matrix, architecture, coverage and remaining work aligned with the evidence boundaries |
| Final verification | Complete | Links, source/parameter checks, complete visual inventory, unchanged screenshot hashes and final diff reviewed |

## Interpretation

Evidence ledgers distinguish **Full**, **Partial** and **Inconclusive** support for a stated observation. Full support is limited to what the capture actually shows; it is not a fresh tenant test or proof of an unshown configuration. Masked identifiers, missing timestamps and incomplete views remain explicit limitations. Historical lab observations and current Microsoft guidance are recorded separately.

## Code findings and local verification

| Confirmed problem | Correction | Verification / boundary |
| --- | --- | --- |
| Scripts 03/04 selected a group by nonunique display name without checking group type; a same-name dynamic, synchronized, mail-enabled or role-assignable group could be accepted or targeted | Enumerate all matching pages and require the intended assigned cloud security-group type before reuse or membership changes | Offline duplicate/type cases reject without writes; sequential user/membership/role reruns verified |
| Script 06 could report completion after omitted memberships or a failed/skipped CA read; CSVs did not retain enough IDs to identify unresolved principals or duplicate names | Persist `InProgress`, `Complete`, `Partial` or `Failed` with omitted-section identifiers; retain stable IDs in CSVs | Complete, partial membership, CA failure, missing CA scope and terminating-read failure cases verified locally |
| Script 06 used a second-resolution output directory with `-Force`, allowing a same-second rerun to overwrite an earlier export | Include a unique run ID and print the exact output directory | Repeated local exports produce distinct directories |

`pwsh -NoProfile -File ./tests/local/Test-GraphScripts.ps1` passed **54 assertions**, including parsing all six source scripts. The harness loads no Graph SDK, disables SDK autoloading and resolves every Graph command to a local function before execution. It is not a Graph integration test. Scripts 01, 02 and 05 required no source correction; existing operator/target guards, secure password prompt, default role dry run, explicit confirmation and propagated write errors were retained.

The six KQL files were checked with **Microsoft.Azure.Kusto.Language 12.4.1**, `KustoCode.ParseAndAnalyze`, and a local schema containing the referenced documented `SigninLogs` and `AuditLogs` columns. All six returned **zero diagnostics**. This validates syntax and static column/types for that schema, not data ingestion, retention, workspace authorization or runtime results. No query was executed against Azure. References: [Kusto parser](https://learn.microsoft.com/en-us/kusto/api/netfx/kusto-language-parse-queries?view=microsoft-fabric), [SigninLogs schema](https://learn.microsoft.com/en-us/azure/azure-monitor/reference/tables/signinlogs), [AuditLogs schema](https://learn.microsoft.com/en-us/azure/azure-monitor/reference/tables/auditlogs).

The exporter remains a selected summary, not a backup or effective-permission calculation. Direct membership API limitations, non-user placeholders and missing PIM eligibility are documented in [script usage](../scripts/README.md). Revised scripts require a deliberate tenant retest before any new lab Pass can be claimed.

## Evidence and documentation findings

All **75 baseline text files** were reviewed: the 54 day documentation/test/evidence indexes, seven other Markdown files, six scripts, six queries, SVG and `.gitignore`. All **258 screenshots** were visually inspected in day batches and appear exactly once in the three linked ledgers. Original numbering, filenames (including historical spelling/double-dot quirks), masking and `BFL-DC01` were preserved.

The corrections address these concrete mismatches:

- Unshown fresh sign-ins, TAP use, application business operations and AU mutations were presented more strongly than their evidence permits. Displayed role labels, settings and enabled buttons now have explicit runtime boundaries.
- The Day 06 AU group view does not establish direct user membership; offboarding leaves a license and has no session-revocation proof. The generic account-locked message does not identify the exact sign-in error cause.
- Day 07's Connect completion screen says synchronization was initiated, not that the first export completed. Password-change propagation is unproven. Day 08 screenshots from different sign-ins no longer imply exact-event correlation.
- Day 09 policy-creation attribution and Day 11 access between Deny and Apply lack the necessary actor/timestamp/fresh-session evidence. Day 10's recorded end-time typo was corrected to `10:20:44 AM`.
- Day 12 token acquisition/client attribution and full credential/consent cleanup are not all visible. Day 18 confirms selected later credential/consent state; it does not retroactively demonstrate fresh Easy Auth code redemption or renewal.
- Day 13's handled write-denial PASS and Day 14's successful target session do not independently validate the expected Storage error or password replay. Missing source and concrete retests are recorded.
- Day 18's custom effective-exclusion output lacks the source/membership inputs needed to verify its calculation. A blanked Storage banner cannot prove that an earlier warning was resolved. CA sign-in and KQL screenshots corroborate policy outcomes for different events.

Existing correct material was retained, including the Day 15/16 source documents and all six KQL queries. No tenant test was promoted to Pass through a documentation or code change. Current [SC-300 objectives and licensing prerequisites](sc-300-coverage.md) were checked against Microsoft Learn, separately from the historical lab. The original P2 tenant label does not establish guest billing, GSA or Defender entitlements.

## Remaining verification boundary

The audit cannot inspect the current live tenant, original application/Runbook/Workbook source absent from the repository, masked identifiers, unrecorded token/session transitions or actual license assignments. It is a repository/evidence audit, not a live security certification. [Remaining work](remaining-work.md) lists the exact lab operations and captures needed to resolve those gaps; it also retains previously documented SSPR, monitoring and policy follow-ups.

## Final checks and publication record

- **54 offline assertions passed**, independently rerun during code review; no additional actionable code finding.
- **Seven PowerShell documentation blocks** parsed without errors. The named parameters at **27 Graph command call sites** matched the installed SDK metadata; the commands themselves were not executed. Metadata checks are not API integration tests.
- **Six KQL queries**: zero local syntax/semantic diagnostics and exact visual correspondence to the six published query captures.
- **822 relative Markdown/HTML links and anchors** and **292 inline image filename references** resolved. All **31 distinct external link URLs** returned HTTP 200 (following redirects).
- **258/258 images** occur exactly once in the evidence ledgers, and all **258 original SHA-256 hashes** are unchanged.
- Final diff checked for unintended files, unsupported Pass changes and whitespace errors, allowing the repository's existing two-space Markdown line breaks. No screenshot, KQL source, tenant export or credential was added or altered.

Stages were committed separately and published to `audit/sc300-evidence-2026-09-25` through the GitHub connector because the local Git credential helper could not authenticate a push. Each published commit tree was compared with its corresponding local tree. The associated Pull Request is the final review record; `main` was not modified.
